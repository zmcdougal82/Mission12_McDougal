{
  "dependencies": {
    "axios": "^1.8.4",
    "bootstrap": "^5.3.3"
  },
  "scripts": {
    "build": "cd bookstore-client && npm ci && npm run build && cp public/routes.json build/",
    "build:azure": "cd bookstore-client && npm ci && npm run build && cp public/routes.json build/"
  }
}
